<?php

/*
|--------------------------------------------------------------------------
| باشگاه اعضا — پیش‌فرض‌های موتور امتیاز
|--------------------------------------------------------------------------
| این‌ها فقط مقادیر جایگزین (fallback) هستند؛ هر جا قانون امتیاز متناظری در
| جدول point_rules تعریف شده باشد (پنل مدیریت ← باشگاه اعضا ← قوانین امتیاز)
| مقدار دیتابیس مقدم است — تصمیم نهایی ۵-۳ نقشه راه: نرخ‌ها و جوایز کاملاً
| قابل‌تعریف توسط مدیر.
*/

return [
    // منطقه زمانی محاسبه «روز» برای زنجیره ورود، سقف روزانه و چرخش رایگان
    'timezone' => 'Asia/Tehran',

    // نقاط عطف زنجیره ورود که جایزه streak_bonus می‌گیرند (روز)
    'streak_milestones' => [7, 30],

    // تعداد چرخش رایگان چرخ شانس در هر روز
    'wheel_free_spins_per_day' => 1,

    // هزینه هر چرخش اضافه (امتیاز) — قانون فعال با کد wheel_cost آن را بازنویسی می‌کند
    'wheel_cost' => 100,

    // مدت اعتبار cache موجودی امتیاز (ثانیه)
    'balance_cache_ttl' => 60,

    // تعداد ردیف در هر صفحه تاریخچه امتیاز
    'history_per_page' => 20,
];
